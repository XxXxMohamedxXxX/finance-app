name: Build APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Init TWA (Non-interactive)
        run: |
          npx bubblewrap init \
            --manifest=https://finance-manager-pro-1--mohamedmagedpro.replit.app/manifest.json \
            --directory=twa \
            --packageId=com.finance.app \
            --host=finance-manager-pro-1--mohamedmagedpro.replit.app \
            --name="Finance Manager" \
            --launcherName="Finance Manager" \
            --display=standalone \
            --themeColor=#000000 \
            --backgroundColor=#000000 \
            --navigationColor=#000000 \
            --navigationColorDark=#000000 \
            --orientation=portrait

      - name: Build APK
        run: |
          cd twa
          ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: twa/app/build/outputs/apk/release/app-release.apk
